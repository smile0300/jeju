# 回国红包 구현 스펙 — 해결방안 상세

- **작성일**: 2026-08-21
- **선행 문서**: `jeju-live-reward-activation-plan.md` (전략)
- **본 문서**: 실제 구현 가능한 수준의 데이터 모델 · 로직 · 화면 · 문안 · 운영 SOP

---

## 0. 확인된 현재 구조 (실측)

네트워크와 API 응답을 직접 확인한 결과입니다.

### 0-1. 백엔드 실체

| 항목 | 확인 내용 |
|---|---|
| 자체 API | `/api/reward-list`, `/api/success-list`, `/api/public-data`(공공API 프록시) |
| 데이터 저장소 | **Google Sheets 기반으로 추정** — 평면 스키마, `ItemImg`가 Google Drive 링크, 한국어 원본 + `_zh`/`_en` 번역 컬럼 구조 |
| 기존 스키마 (`success-list`) | `CaseId`(JEJU-0001) · `Step`(1~4) · `Date` · `WeChatId` · `Item` · `Region`(중국 도시) · `Place`(습득장소) · `ItemImg` · `Note` + `_zh`/`_en` |

**좋은 소식이 두 개 있습니다.**

1. **`Step` 필드가 이미 존재**합니다 — 상태 머신의 뼈대가 이미 있습니다. 확장만 하면 됩니다.
2. **`Region`에 중국 목적지 도시가 이미 수집되고 있습니다**(쓰촨·충칭·상하이). 매칭 축의 절반이 이미 데이터로 존재하는데 **화면에 안 쓰고 있을 뿐**입니다.

### 0-2. 🔴 즉시 수정 — 버그 3건 (원인 특정 완료)

#### ① 깨진 이미지 — 원인 규명

`ItemImg`에 저장된 값이 이것입니다.

```
https://drive.google.com/file/d/14YbNME47r3PA_mhlhZc2Z8lk52fiSsqZ/view?usp=drive_link
```

이건 **뷰어 페이지 URL이지 이미지 파일 URL이 아닙니다.** `<img src>`에 넣으면 100% 깨집니다. 신뢰를 요구하는 페이지의 첫 카드가 깨져 보이는 원인이 이것입니다.

**수정 (프론트에서 변환)**

```js
// Drive 공유 링크 → 실제 이미지 URL
function driveImg(url, w = 400) {
  const m = String(url || '').match(/\/file\/d\/([^/]+)/) || String(url || '').match(/[?&]id=([^&]+)/);
  return m ? `https://drive.google.com/thumbnail?id=${m[1]}&sz=w${w}` : '';
}

// 사용
const src = driveImg(item.ItemImg);
img.src = src || FALLBACK_ICON;      // 없으면 카테고리 아이콘
img.onerror = () => { img.src = FALLBACK_ICON; };
```

**전제 조건**: Drive 파일 공유 권한이 `링크가 있는 모든 사용자`여야 합니다. 하나라도 `제한됨`이면 그 건만 깨집니다.

**단, 이건 임시방편입니다.** Drive 핫링크는 트래픽이 늘면 레이트리밋에 걸립니다. **Phase 1 내에 오브젝트 스토리지(S3/R2/Supabase Storage)로 이전**하고 `ItemImg`에는 CDN URL을 저장하세요.

#### ② 🔴 개인정보 노출 — 微信 ID가 인증 없이 공개

`https://jeju-live.com/api/success-list` 를 **누구나 브라우저에서 열면** 실제 사용자의 微信 ID와 성명이 그대로 나옵니다.

```json
{"CaseId":"JEJU-0001","WeChatId":"liang ke","Item":"잠옷","Region":"쓰촨", ...}
```

微信 ID는 **직접 연락이 가능한 식별자**입니다. 스팸·사칭·표적 접근에 그대로 노출됩니다. 성공 사례 모달을 띄우려고 전체 원본을 내려주는 구조로 보입니다.

**수정**
- 공개 엔드포인트 응답에서 `WeChatId` **제거**. 표시용 마스킹 값(`li***ke`)을 서버에서 만들어 별도 필드로 내려주세요
- `Note` 필드도 내부 메모가 들어갈 수 있으므로 공개 응답에서 제외
- 향후 운반자·소유자 연락처는 **매칭 성사된 당사자에게만** 서버에서 조건부 노출

> 이건 기능 개선이 아니라 **지금 존재하는 노출**입니다. 다른 무엇보다 먼저 처리하세요.

#### ③ 🟠 홍바오 페이지에서 항공 API 9페이지 폴링

`/reward` 페이지에 머무는 동안 **60초마다 `flight-status/arrival` 9페이지를 반복 호출**하는 것을 확인했습니다. QA 리포트의 백그라운드 폴링 문제가 이 페이지에서도 그대로 재현됩니다.

**중요**: 아래 매칭 기능은 항공 데이터를 더 쓰게 됩니다. **폴링을 먼저 잡지 않으면 API 일일 한도를 태우고 전체 장애로 이어집니다.** 3-1의 캐시 설계를 반드시 선행하세요.

### 0-3. 🟢 보너스 — 기상특보 API는 이미 붙어 있습니다

날씨 기획서에서 "최우선"으로 제안했던 기상특보가 **이미 호출되고 있습니다.**

```
/api/public-data?endpoint=.../WthrWrnInfoService/getWthrWrnList&stnId=184
/api/public-data?endpoint=.../WthrWrnInfoService/getWthrWrnMsg&stnId=184
```

`stnId=184`는 제주입니다. **데이터는 이미 들어오는데 화면에 표시만 안 하고 있습니다.** 날씨 기획서의 2-1 항목은 난이도 **M → S**로 내려갑니다. 표시 레이어만 만들면 됩니다.

---

## 1. 데이터 모델

기존 Sheets 구조를 유지하면서 확장하는 안입니다. (DB 이전 시에도 그대로 테이블로 변환 가능)

### 1-1. `reward_items` — 운반 요청 (기존 시트 확장)

| 컬럼 | 타입 | 설명 | 신규 |
|---|---|---|---|
| `CaseId` | string | JEJU-0001 | 기존 |
| `Step` | int | 상태 (1-2 참조) | 기존/확장 |
| `Item` / `_zh` / `_en` | string | 물품명 | 기존 |
| `ItemImg` | url | **CDN URL로 전환** | 기존/수정 |
| `Region` / `_zh` / `_en` | string | 중국 목적지 도시 | 기존 |
| `Place` / `_zh` / `_en` | string | 습득 장소 | 기존 |
| `OwnerWeChat` | string | **비공개** — 매칭 후에만 노출 | 기존(보안수정) |
| **`DestCityCode`** | string | 도시 IATA (PVG/PEK/CKG…) — **매칭 키** | ★신규 |
| **`Deadline`** | date | 운반 마감일 | ★신규 |
| **`SizeClass`** | enum | S / M / L | ★신규 |
| **`WeightG`** | int | 무게(g) | ★신규 |
| **`ValueRMB`** | int | 신고 가액 (상한 검증용) | ★신규 |
| **`RewardRMB`** | int | 보상금 (자동 산정) | ★신규 |
| **`EscrowStatus`** | enum | none / held / released / refunded | ★신규 |
| **`CarrierId`** | string | 매칭된 운반자 | ★신규 |
| **`LostRefNo`** | string | LOST112 접수번호 (세관 증빙용) | ★신규 |
| **`HasBattery`** | bool | 리튬배터리 포함 → 기내수하물 강제 | ★신규 |

### 1-2. 상태 머신 (`Step` 확장)

현재 `Step`은 1~4로 쓰이고 있습니다. 아래로 확장하세요.

```
 0  DRAFT        등록 접수, 검수 대기
 1  LISTED       검수 통과, 게시중          ← 운반자에게 노출 시작
 2  ESCROWED     소유자 보상금 예치 완료      ← 이게 있어야 운반자가 신뢰함
 3  MATCHED      운반자 확정
 4  HANDED_OVER  대면 인계 완료 (사진 필수)
 5  DEPARTED     탑승/출국 확인
 6  SHIPPED      중국 국내 택배 발송 (운송장번호)
 7  DELIVERED    소유자 수령 확인
 8  SETTLED      보상금 지급 완료
──────────────────────────────────
X1  CANCELLED    취소 (예치금 환불)
X2  DISPUTED     분쟁 (수동 처리)
X3  RETURNED     운반 실패, 물품 회수
```

**규칙**
- `1 → 2` 없이 `3`(매칭)으로 갈 수 없음. **예치가 매칭의 선행 조건**
- `4`(인계)는 **사진 업로드 없이 진행 불가** — 시스템에서 강제
- `6 → 7`은 소유자 확인 또는 **발송 후 7일 자동 확정**(무응답 방지)
- `8`은 `7` 이후 자동 실행

### 1-3. `carriers` — 운반자 (신규 시트)

| 컬럼 | 설명 |
|---|---|
| `CarrierId` | 고유 ID |
| `NameMasked` | 표시용 (`王**`) |
| `PassportHash` | **여권번호 SHA-256 해시** — 원본 저장 금지, 중복 탐지용 |
| `WeChatId` | 비공개 |
| `FlightNo` / `DepDate` / `DestCityCode` | 항공편 정보 |
| `VerifyStatus` | none / phone / realname |
| `TripCount` | 누적 운반 횟수 |
| `LastTripAt` | 최근 운반일 — **1인 1건 판정용** |
| `SuccessCount` / `DisputeCount` | 신뢰 점수 산출용 |

---

## 2. 매칭 엔진

### 2-1. 필수 조건 (하나라도 불일치 시 후보 제외)

```
① DestCityCode 일치       (또는 동일 성(省) 내 + 소유자 동의)
② DepDate ≤ Deadline
③ SizeClass 수용 가능
④ 운반자 1인 1건 제한 통과
⑤ ValueRMB ≤ 2,000
```

### 2-2. 우선순위 점수 (후보 정렬)

```js
function matchScore(item, carrier) {
  let s = 0;

  // 시간 여유 — 마감 임박 건을 먼저 소진
  const slack = daysBetween(carrier.DepDate, item.Deadline);
  s += slack <= 1 ? 40 : slack <= 3 ? 30 : slack <= 7 ? 20 : 10;

  // 신뢰도
  if (carrier.VerifyStatus === 'realname') s += 30;
  else if (carrier.VerifyStatus === 'phone') s += 10;
  s += Math.min(carrier.SuccessCount * 10, 30);
  s -= carrier.DisputeCount * 20;

  // 직항 여부 (환승은 분실 위험 ↑)
  if (isDirectFlight(carrier.FlightNo)) s += 15;

  // 크기 적합
  if (item.SizeClass === 'S') s += 10;

  return s;
}
```

### 2-3. 항공편 검증 — 기존 자산 활용

운반자가 편명(`MU5038`)을 입력하면:

1. `/api/public-data?endpoint=.../flight-status/departure&schDate=YYYYMMDD` 조회
2. 편명 매칭 → **목적지 도시 코드 자동 추출** (수기 입력 오류 제거)
3. 결항·지연 시 매칭된 소유자에게 자동 알림

**캐시 필수**: 출발편 목록은 **1시간 TTL**로 서버 캐시. 0-2 ③의 폴링 문제와 함께 처리하세요.

```
GET /api/flights/departures?date=20260825&country=CN
→ 서버 캐시 (TTL 3600s) → 중국행만 필터 → [{flightNo, destCode, destName, schedTime}]
```

### 2-4. 알림 트리거

| 시점 | 대상 | 내용 |
|---|---|---|
| 신규 물품 등록 | 해당 도시행 등록 운반자 | `上海行 새 요청 · 120 RMB` |
| 매칭 성사 | 양측 | 상대 연락처 + 인계 안내 |
| 마감 D-2 미매칭 | 소유자 | 보상금 상향 제안 |
| 인계 후 24h 미탑승 | 운영자 | 이상 감지 |
| 발송 후 7일 미확인 | 소유자 | 자동 확정 예고 |

> **채널 주의**: 타깃이 微信 인앱 브라우저라 **Web Push가 동작하지 않습니다.** 微信 공식계정 템플릿 메시지 또는 SMS를 기본 채널로 설계하세요. 이건 기술 선택이 아니라 **작동 여부**의 문제입니다.

---

## 3. 화면 스펙

### 3-1. 운반자 온보딩 — 3단계 (신규)

**진입점**: 히어로 `我要带回国` 버튼

```
[STEP 1] 항공편 확인
 ├ 편명 입력 (자동완성)  또는  날짜 + 도시 선택
 ├ → 항공 API 검증 → 목적지 자동 표시
 └ ✓ MU5038 · 8/25 14:20 · 济州 → 上海浦东

[STEP 2] 본인 확인
 ├ 성명 (여권 표기와 동일)
 ├ 여권번호 (해시 저장, 원본 미저장 명시)
 ├ 微信 ID
 └ 휴대폰 인증

[STEP 3] 운반 규칙 동의  ← 체크박스 개별 동의 (일괄 동의 금지)
 ├ □ 물건은 반드시 열어서 내용물을 확인한 뒤 받겠습니다
 ├ □ 밀봉된 물건은 절대 받지 않겠습니다
 ├ □ 전자기기는 기내 수하물로만 운반하겠습니다
 ├ □ 1회 1건만 운반하겠습니다
 └ □ 세관 문의 시 제공된 증명서를 제시하겠습니다
```

**설계 의도**: STEP 3을 개별 체크로 두는 건 UX가 아니라 **책임 소재를 명확히 하기 위한 장치**입니다. 일괄 동의로 만들지 마세요.

### 3-2. 물품 카드

```
┌────────────────────────────────────┐
│ [사진]              ⏱ 8/25까지 (D-4) │
│ Apple Watch SE                      │
│ 📍 上海浦东 PVG                       │
│ 📦 소형 · 120g · 기내수하물           │
│ 💰 120 RMB    ✅ 보상금 예치완료       │
│ 🔒 실명확인 소유자 · 개봉 인계          │
│ [ 接单 (수락) ]                      │
└────────────────────────────────────┘
```

**필수 변경점**: 현재 카드는 `물품명 + 보상금` 뿐입니다. **목적지·마감일·예치여부** 세 가지가 없으면 운반자가 판단할 수 없습니다.

### 3-3. 필터 바

```
[목적지 도시 ▾] [출발일 ▾] [크기 ▾]   정렬: 보상금 높은순 / 마감 임박순
```

로그인 상태면 **내 항공편 기준 자동 필터**가 기본값.

### 3-4. 소유자 등록 폼

기존 `发布咨询`을 확장:

```
① 물품 정보    사진(필수) · 물품명 · 카테고리 · 대략 가액
② 수령 정보    중국 도시(IATA 자동매핑) · 수령 희망 마감일
③ 분실 증빙    LOST112 접수번호 또는 분실 경위
④ 보상금       자동 산정 결과 표시 + 국제택배 비교
⑤ 예치         微信 결제 → EscrowStatus = held
```

**④에 비교표를 반드시 노출하세요.**

```
国际快递   约 150 RMB · 7~10일 · 통관 리스크
红包 운반   120 RMB · 2~3일  ✅ 30 RMB 절약 + 5일 단축
```

*※ 국제택배 요율은 실사 후 확정할 것 (본 문서 수치는 추정)*

---

## 4. 보상금 산정 공식

```js
function calcReward({ sizeClass, daysLeft, destTier, weightG }) {
  let base = 60;                                    // 기본료

  base += { S: 0, M: 20, L: 50 }[sizeClass];        // 부피

  base += daysLeft <= 3 ? 30                        // 긴급도
        : daysLeft <= 7 ? 10 : 0;

  base += destTier === 'direct' ? 0 : 20;           // 직항 없는 도시

  const shipping = 20;                              // 중국 국내택배 (소유자 부담)

  return { reward: base, shipping, total: base + shipping };
}
```

**계산 예시**

| 사례 | 산식 | 보상금 | 소유자 총액 |
|---|---|---|---|
| AirPods · 상하이 · 여유 5일 | 60+0+10+0 | **70** | 90 RMB |
| Apple Watch · 충칭(환승) · 3일 | 60+0+30+20 | **110** | 130 RMB |
| 백팩 · 베이징 · 여유 10일 | 60+20+0+0 | **80** | 100 RMB |

**현재 50 RMB와 비교**: 운반자 실수령이 30 RMB → **70~110 RMB**로 올라갑니다. 소유자는 여전히 국제택배(추정 150 RMB) 대비 이득입니다. **양쪽이 동시에 이득인 구간이 존재하며, 현재 가격은 그 구간 밖에 있습니다.**

**플랫폼 수수료**: 초기 **0%** (유동성 우선) → 성사 100건 이후 소유자에게 15% 별도 부과.

---

## 5. 에스크로 · 정산 규칙

| 이벤트 | 처리 |
|---|---|
| 소유자 예치 | `EscrowStatus = held`, 카드에 `✅ 예치완료` 배지 |
| 정상 완료 (Step 7) | 운반자에게 전액 지급, `released` |
| 발송 후 7일 무응답 | **자동 확정** 후 지급 (소유자 잠수 방지) |
| 매칭 전 소유자 취소 | 전액 환불 |
| 매칭 후 소유자 취소 | 운반자에게 **위약금 30 RMB** 지급 후 잔액 환불 |
| 운반자 인계 후 미탑승 | 물품 회수, 미지급, `DisputeCount +1` |
| 세관 압수 | **운반자 무과실** → 보상금 전액 지급 + 소유자 환불은 별도 보험/플랫폼 부담 |
| 파손 | 사진 증빙 비교 후 부분 지급 |

> **세관 압수 조항이 핵심입니다.** 운반자가 가장 두려워하는 시나리오이고, "당신 책임이 아니다"를 약관에 명문화하는 것만으로 참여 장벽이 크게 낮아집니다.

---

## 6. 세관 · 보안 대응 키트

### 6-1. 분실물 증명서 PDF (자동 생성)

매칭 확정 시 운반자에게 **PDF + 이미지** 두 형태로 발급. 오프라인에서도 열리도록 이미지 필수.

```
────────────────────────────────────
   遗失物品携带证明 / 분실물 운반 증명서
   Lost Property Transport Certificate
────────────────────────────────────
 관리번호   JEJU-0042
 물품       Apple Watch SE (사용품)
 [물품 사진]
 분실장소   제주국제공항 / 济州国际机场
 분실일자   2026-08-15
 접수번호   LOST112 - 2026-xxxxx
 소유자     王** (본인 소유 물품)
 운반자     李** (무상 운반, 상업 목적 아님)
 ────────────────────────────────
 本物品为失主本人在韩国济州岛遗失的
 个人使用物品，由携带人无偿协助带回。
 非商业用途，非代购。
 ────────────────────────────────
 문의  jeju-live.com  |  +82-XX-XXXX-XXXX
```

### 6-2. 세관 질문 시 응답 문안 (운반자용 카드)

**中文**
> 这是我朋友在韩国济州岛遗失的个人物品，我帮忙带回国。
> 这是遗失物证明和失主信息。不是代购，也不是新品。

**한국어 (출국 시)**
> 제주에서 분실된 물품을 소유자에게 전달하기 위해 운반 중입니다. 분실물 증명서를 소지하고 있습니다.

**English**
> This is a personal item lost by its owner in Jeju. I'm returning it to them. Here is the lost-property certificate.

### 6-3. 운반자 FAQ (페이지에 반드시 게시)

| 질문 | 답변 |
|---|---|
| 밀봉된 물건인가요? | **아니요.** 반드시 열어서 내용물을 직접 확인한 뒤 받습니다. 밀봉 상태로는 전달하지 않습니다. |
| 세관에서 물어보면? | 분실물 증명서를 제시하세요. 상업 목적이 아닌 개인 분실물 반환입니다. |
| 압수당하면 제 책임인가요? | **아닙니다.** 보상금은 정상 지급되며 책임을 묻지 않습니다. |
| 위탁수하물에 넣어도 되나요? | **안 됩니다.** 전자기기는 리튬배터리 규정상 기내 수하물만 가능합니다. |
| 여러 건 받아도 되나요? | **1회 1건만 가능**합니다. 시스템에서 제한됩니다. |
| 보상금은 언제 받나요? | 소유자가 미리 예치합니다. 발송 확인 후 즉시 지급됩니다. |

---

## 7. 1인 1건 강제 — 구현

전략 문서에서 "생존선"이라 한 규칙의 실제 구현입니다.

```js
const COOLDOWN_DAYS = 14;   // 동일인 재운반 최소 간격
const MAX_PER_MONTH  = 2;

function canAccept(carrier, now) {
  if (carrier.activeTrip) return { ok:false, reason:'ACTIVE_TRIP' };
  if (daysSince(carrier.LastTripAt) < COOLDOWN_DAYS) return { ok:false, reason:'COOLDOWN' };
  if (carrier.tripsThisMonth >= MAX_PER_MONTH) return { ok:false, reason:'MONTH_LIMIT' };
  return { ok:true };
}
```

**우회 탐지** — 계정만 막으면 새 계정으로 우회합니다. 아래를 **동일인 판정 키**로 함께 봅니다.

- `PassportHash` (원본 미저장, 해시만) — **가장 강력**
- `WeChatId`
- 휴대폰 번호
- 동일 편명 + 동일 성씨 반복

**운영 알림**: 동일 키로 월 3회 이상 시도 시 운영자 대시보드에 플래그. **차단보다 먼저 사람이 확인**하는 절차를 두세요 — 오탐으로 정상 사용자를 막으면 초기 유동성에 치명적입니다.

---

## 8. 분쟁 처리 SOP

| 시나리오 | 판정 | 조치 |
|---|---|---|
| 운반자 잠수 (인계 후 연락 두절) | 운반자 과실 | 미지급 · 계정 정지 · 소유자 전액 환불 · 경찰 신고 검토 |
| 물품 파손 | 사진 비교 | 인계 사진에 없던 손상이면 부분 지급(50%) |
| 세관 압수 | **무과실** | 보상금 전액 지급 · 소유자 환불(플랫폼 부담) |
| 소유자 수령 후 미확인 | 7일 자동 확정 | 지급 진행 |
| 소유자 "안 받았다" 주장 | 운송장 추적 | 배송완료 기록 있으면 지급 |
| 물품이 설명과 다름 | 인계 사진 확인 | 소유자 귀책 시 운반자에게 지급 |
| 운반자 미탑승 (결항 등) | 무과실 | 물품 회수 · 재매칭 · 소액 보상 |

**증빙 3종이 모든 판정의 근거입니다.** 인계 사진 · 탑승권 · 운송장. 이 3개가 없으면 분쟁을 판정할 수 없으므로 **시스템에서 필수 단계로 강제**하세요.

---

## 9. 초기 시딩 플레이북 (첫 20건)

현재 2건 → **20건**을 만드는 구체적 순서입니다.

### Week 1 — 재고 확보
- `代为领取`(대리수령) 신청 이력에서 **중국 귀국자 건 전수 추출**
- 이미 회수됐지만 미전달 상태인 물품 목록화
- 소유자에게 개별 연락: "무료로 전달해드립니다" (초기엔 **보상금 전액을 플랫폼이 부담**)
- 목표: **10건 확보**

### Week 2 — 노출
- 분실물 페이지 검색 결과에 배너 삽입 (아래 3-6 연계)
- 항공 페이지(출발편)에 운반자 모집 배너
- 목표: 자연 유입 **5건**

### Week 3 — 운반자 확보
- 제주 여행 微信 그룹 · 小红书에 모집글
- **초기 보상금 150 RMB로 상향** (마케팅비로 처리)
- 목표: 운반자 **10명 등록**, 성사 **3건**

### Week 4 — 콘텐츠화
- 성사 3건을 사진·후기와 함께 페이지 상단 고정
- 小红书 후기 포스팅 (스토리가 강한 소재입니다)
- 목표: 게시 **20건 유지**

**초기 비용 추정**: 20건 × 150 RMB ≈ **3,000 RMB (약 60만원)**. 양면 시장 부트스트랩 비용으로는 매우 저렴한 편입니다.

---

## 10. 개발 티켓

### Sprint 0 — 긴급 (2~3일) 🔴
| # | 작업 | 공수 |
|---|---|---|
| 1 | `success-list`에서 `WeChatId`·`Note` 제거, 마스킹 필드 추가 | 0.5d |
| 2 | Drive 이미지 URL 변환 + onerror 폴백 | 0.5d |
| 3 | `/reward`에서 항공 폴링 중단 (라우트 언마운트 cleanup) | 1d |
| 4 | 항공 API 서버 캐시 (TTL 1h) | 1d |

### Sprint 1 — 매칭 (2주)
| # | 작업 | 공수 |
|---|---|---|
| 5 | 시트 스키마 확장 (1-1) | 1d |
| 6 | `/api/flights/departures` 중국행 필터 + 캐시 | 2d |
| 7 | 운반자 등록 3단계 폼 | 3d |
| 8 | 매칭 엔진 (2-1, 2-2) | 3d |
| 9 | 카드 재설계 + 필터 바 | 2d |
| 10 | 소유자 등록 폼 확장 + 보상금 자동 산정 | 2d |

### Sprint 2 — 신뢰 (2주)
| # | 작업 | 공수 |
|---|---|---|
| 11 | 상태 머신 (1-2) + 단계별 추적 화면 | 3d |
| 12 | 에스크로 (微信 결제 연동) | 4d |
| 13 | 인계 사진 업로드 강제 | 2d |
| 14 | 분실물 증명서 PDF 자동 생성 | 2d |
| 15 | 1인 1건 제한 + 우회 탐지 | 2d |
| 16 | FAQ 페이지 (中/韓/英) | 1d |

### Sprint 3 — 유입 (1주)
| # | 작업 | 공수 |
|---|---|---|
| 17 | 분실물 페이지 연계 배너 | 1d |
| 18 | 항공 페이지 운반자 모집 배너 | 1d |
| 19 | 홈 카드 실시간 건수 뱃지 | 0.5d |
| 20 | 성사 사례 섹션 | 1.5d |

---

## 11. 우선 처리 3가지

문서가 길어졌으니 **당장 할 것**만 다시 정리합니다.

1. **`WeChatId` 공개 노출 차단** — 오늘 처리 가능하고, 지금 실제로 노출 중입니다
2. **Drive 이미지 URL 변환** — 반나절이면 되고, 페이지 첫인상을 좌우합니다
3. **`Region` → 카드에 목적지 표시** — 데이터는 이미 있습니다. 매칭의 절반이 표시만으로 해결됩니다

이 세 개는 Phase 0(법률 검토)와 무관하게 지금 진행해도 되는 항목입니다. 나머지 Sprint 1 이후는 **국제물류주선업 등록 필요 여부 결론이 난 뒤** 착수하시길 권합니다.
